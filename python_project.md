create a python project.
create a venv.
set up VS Code to automatically activate the virtual environment when you open a terminal. 
prepare files to release it as a exe file for windows.

this project is to do the following:
